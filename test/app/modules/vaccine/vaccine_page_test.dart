import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:appmodularmobx/app/modules/vaccine/vaccine_page.dart';

void main() {
  testWidgets('VaccinePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VaccinePage(title: 'Vaccine')));
    //  final titleFinder = find.text('Vaccine');
    //  expect(titleFinder, findsOneWidget);
  });
}
